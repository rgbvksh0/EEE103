#include<stdio.h>

// write a program to find the smallest among three numbers using pointers.
// write a function to accept a string and count the number of vowels present in the string

int main()
{
    int a, b, c;
    int *p1 = &pa;
    int *p2 = &pb;
    int *p3 = &pc;

    printf("Enter three numbers: ");

    scanf("%d", pa);

    scanf("%d", pb);

    scanf("%d", pc);

    if(*pa>pb)
    {
        if
    }


}
